# Prototype only:
# GEMINI_API_KEY is currently used by the Android client demo flow.
# This should not remain the production architecture.
# Production should move model access behind a server-side advisory service.
GEMINI_API_KEY=MY_GEMINI_API_KEY
